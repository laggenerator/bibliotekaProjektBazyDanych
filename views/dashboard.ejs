        <% if (locals.error) { %>
            <div class="alert alert-error">
                <%= locals.error %>
            </div>
        <% } %>

        <% if (locals.sukces) { %>
            <div class="alert alert-sukces">
                <%= locals.sukces %>
            </div>
        <% } %>

<div class="dashboard-container">
    <div class="dashboard-header">
        <h1>Panel użytkownika</h1>
        <p>Witaj, <strong class="<%= locals.uzytkownik.rola === 'ADMIN' ? 'rola-ADMIN' : 'rola-USER-light' %>"><%= locals.uzytkownik.nazwa_uzytkownika %></strong>!</p>
    </div>

    

    <div class="dashboard-sections">
      <div class="section-card">
        <h2>Szybkie akcje</h2>
        <div class="action-buttons">
          <a href="/ksiazki" class="action-btn">
            <span class="action-icon">🔍</span>
            <span>Przeglądaj książki</span>
          </a>
          <a href="/dashboard/biezace-wypozyczenia" class="action-btn">
            <span class="action-icon">📖</span>
            <span>Bieżące wypożyczenia</span>
          </a>
          <a href="/dashboard/historia" class="action-btn">
            <span class="action-icon">📊</span>
            <span>Historia wypożyczeń</span>
          </a>
          <a href="/dashboard/moje-recenzje" class="action-btn">
            <span class="action-icon">⭐</span>
            <span>Moje recenzje</span>
          </a>
        </div>
      </div>

      <div class="section-card">
        <h2>Informacje o koncie</h2>
        <div class="action-buttons">
          <div class="info-item">
            <span class="info-label">Numer karty:</span>
            <span class="info-value"><%= locals.uzytkownik.numer_karty %></span>
          </div>
          <div class="info-item">
            <span class="info-label">Nazwa użytkownika:</span>
            <span class="info-value"><%= locals.uzytkownik.nazwa_uzytkownika %></span>
          </div>
          <div class="info-item">
            <span class="info-label">Email:</span>
              <span class="info-value"><%= locals.uzytkownik.email %></span>
            </div>
            <div class="info-item">
              <span class="info-label">Data dołączenia:</span>
              <span class="info-value"><%= locals.uzytkownik.datarejestracji %></span>
            </div>
              <form action="/dashboard/dezaktywuj" method="POST" 
                    onsubmit="return confirm('Czy na pewno chcesz dezaktywować swoje konto? Tej operacji nie można samodzielnie cofnąć!')">
                <button type="submit" class="btn btn-danger btn-small">
                  Dezaktywuj konto
                </button>
              </form>
          </div>
        </div>
      </div>
    </div>
      
</div>